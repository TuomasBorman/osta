TODO

- Host documentation on GitHub
- Add function that clean data
- Add function that enriches the data from different data sources
- Add function that analyses the data and creates a dashboard (return also python file so that user can modify it?)


org_name
orrg_number
org id

suppl_name
suppl_id

date

laske total jos ei ole jo

älö katso voucheria

maakoodi täytyy olla listassa

account number on numero

account_name --> str

service_cat --> numero
service_cat_name --> str

Mene rivi kerrallaan, matchaa org ja suppl, muuta nimet yms... --> älä mene, käytä dropduplictas yms

# Check which column is missing if any
col_to_check = ["org_number", "org_name", "org_id"]
# Get columns that are included in data
col_found = (n if n in df.columns else None for n in col_to_check)
# Remove Nones
col_found = [x for x in col_found if x is not None]
# Get columns that are missing from the data
col_missing = set(col_to_check).difference(col_found)
# Subset the data
df_org = df.loc[:, col_found]

org_uniq = df_org.drop_duplicates()
# Check that org_name is correct, org_number is correct, org_id is correct
# Check that there are no NAs
# If there are, fill blanks and assign back
# ORganization was not detected!
